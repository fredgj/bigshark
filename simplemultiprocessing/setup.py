from setuptools import setup

setup(
    name='simplemultiprocessing',
    version='0.3.0',
    author='Fredrik Gjertsen',
    description='Contains an implementation of a process pool that only supports one fuction, map',
    packages=['simplemultiprocessing'],
    )
